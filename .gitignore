####################################################################
# 凭证与本地环境 —— 绝不入库
####################################################################
.env
.env.local
.env.*.local
!.env.example
*.pem
*.key
!**/*.pub
id_rsa*
id_ed25519*
credentials.json
kubeconfig*
.kube/

####################################################################
# 内部工作日志 —— 含实例 ID / VPC / EIP 等账号资源标识，仅本地保留
# 公开版进度记录见 docs/PROGRESS.md（已脱敏）
####################################################################
PROGRESS.local.md
notes.local.md

####################################################################
# Python
####################################################################
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

####################################################################
# 模型权重 / 数据集 / 训练产物（体积大，走 COS 而非 git）
####################################################################
.model_cache/
models/
checkpoints/
outputs/
*.safetensors
*.bin
*.pt
*.ckpt
*.parquet

####################################################################
# 运行时产物
####################################################################
logs/
*.log
!docs/*.log
tracing/
.reward_cache/
nohup.out

####################################################################
# 系统 / 编辑器
####################################################################
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

####################################################################
# 运行产物：含镜像地址（内含账号命名空间），且每次运行都会变
####################################################################
data/criteria_check.json
data/image_map.json
data/sandbox_image_map.json
data/_probe*.json
data/criteria_retry.json
data/criteria_sphinx.json

# 结果归档的中间产物（由 collect_results.py 从节点拉取后生成正式文件）
docs/_*.raw
docs/train_metrics.csv

####################################################################
# 本地工具状态 —— 不随仓库分发
####################################################################
.codebuddy/
